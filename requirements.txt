watchdog>=4.0.0
requests>=2.31.0
plyer>=2.1.0
pystray>=0.19.0
pillow>=10.0.0
flask>=3.0.0
flask-cors>=4.0.0
demoparser2>=0.41.0
# Deps transitivas do demoparser2 — parse_event() tenta polars primeiro,
# pyarrow como fallback. Sem qualquer das duas, o Rust faz .unwrap() num
# Err e estoura PanicException("No module named 'polars'/'pyarrow'").
# PyInstaller não descobre nenhuma das duas porque nada importa direto.
polars>=0.20.0
pyarrow>=15.0.0
pyinstaller>=6.0.0
